#include <iostream>

using namespace std;
struct adat
{
    int honap;
    int nap;
    int hom;
};

int main()
{
    /*
    int n;
    cin >> n;
    adat t[n];
    const int napok[12] = {31,28,31,30,31,30,31,31,30,31,30,31};

    for (int i = 0; i <12; i++)
    {
        cin >> t[i].honap >> t[i].nap >> t[i].hom;
        while (t[i].honap < 1 || t[i].honap > 12 || t[i].nap < 1 || t[i].nap > napok[t[i].honap -1 ])
        {
             cin >> t[i].honap >> t[i].nap >> t[i].hom;
        }

    }
adat maxn = t[0];
for (int i = 1; i < 12; i ++)
{
    if (maxn.hom > t[i].hom)
    {
        maxn = t[i];
    }
}
    cout << maxn.hom << maxn.honap <<endl;
*/





    return 0;
}
